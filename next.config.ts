import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['example.com', 'via.placeholder.com', 'dcdn-us.mitiendanube.com',  'julializie.bwimg.com.br',  "www.madeiramacica.com", "www.shutterstock.com"]},
};

export default nextConfig;
